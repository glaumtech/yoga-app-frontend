import 'package:flutter/material.dart';

/// Navigator for [MaterialApp.router] / [GoRouter] — use when GetX overlay is unavailable.
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
